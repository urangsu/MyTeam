# Live Provider QA Matrix

This matrix records provider behavior by connection state. Live provider QA may remain BLOCKED for main merge review if it is explicitly documented, but release tag remains blocked until live provider QA is closed or the relevant feature is disabled from release surface.

| Case ID | Scenario | Input / Action | Expected result | Forbidden result | Actual result | PASS / FAIL / BLOCKED | Evidence | Notes |
|---|---|---|---|---|---|---|---|---|
| GOOGLE-001 | Calendar no token | `오늘 일정 확인` without Google Calendar token | Google Calendar read connection required; reconnect guidance | Success or empty schedule based only on missing token | Release surface disabled until Google live OAuth QA passes | DISABLED | `ReleaseLiveProviderGate.googleLiveQAPassed = false` | Reason: fail-closed release policy. Next: run controlled OAuth no-token/valid-token/failure QA, then enable gate. |
| GOOGLE-002 | Calendar read success | `오늘 일정 확인` with valid token | Events list or honest empty state after read success | Token presence alone marked as success | Release surface disabled until Google live OAuth QA passes | DISABLED | `ReleaseLiveProviderGate.googleLiveQAPassed = false` | Reason: fail-closed release policy. Next: connect Calendar and run live read QA. |
| GOOGLE-003 | Calendar read failure | Valid-looking token but API failure/forbidden/reauth needed | Permission/reauth guidance; no success state | API failure shown as no events | Release surface disabled until Google live OAuth QA passes | DISABLED | `ReleaseLiveProviderGate.googleLiveQAPassed = false` | Reason: fail-closed release policy. Next: test expired/forbidden token. |
| GOOGLE-004 | Sheets hidden | Inspect Home primary | Google Sheets read not shown as Home primary | Sheets shown as completed primary feature | Static policy checked only | PASS | ProductSurfacePolicy hides spreadsheet.googleSheets.read | Manual UI still recommended. |
| GOOGLE-005 | Sheets missing URL/range | `Google Sheets 읽어줘` | Ask for URL/ID and range; no sample ID/default sheet success | Fake/sample sheet read or write capability | Release surface disabled until Google live OAuth QA passes | DISABLED | `ReleaseLiveProviderGate.googleLiveQAPassed = false` | Reason: fail-closed release policy. Next: run missing-input and read-only live QA before enabling. |
| GOOGLE-006 | Sheets read-only URL/range | `{spreadsheet URL} Sheet1!A1:D20 읽어줘` | Read-only table preview; no write/update/delete wording | 수정/정리 완료 or write action | Release surface disabled until Google live OAuth QA passes | DISABLED | `ReleaseLiveProviderGate.googleLiveQAPassed = false` | Reason: fail-closed release policy. Next: run read-only OAuth live QA. |
| FIN-001 | Samsung stock price | `삼성전자 주가` | Baseline public data, not real-time, not investment advice | 현재가/실시간/추천 wording | Release surface disabled until finance live QA passes | DISABLED | `ReleaseLiveProviderGate.financeLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass Worker health and finance runtime QA before enabling. |
| FIN-002 | KOSPI index | `코스피 지수` | Baseline public index data | Real-time/current index claim | Release surface disabled until finance live QA passes | DISABLED | `ReleaseLiveProviderGate.financeLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass index live QA before enabling. |
| FIN-003 | Samsung 2024 finance summary | `삼성전자 2024 재무요약` | Company finance summary or honest no-result/missing section | Fabricated financials | Release surface disabled until finance live QA passes | DISABLED | `ReleaseLiveProviderGate.financeLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass company statement live QA before enabling. |
| FIN-004 | Finance missing info | `재무상황 알려줘` | Ask for company or use context; no silent Samsung/year default | Silent 삼성전자 2024 default | Release surface disabled until finance live QA passes | DISABLED | `ReleaseLiveProviderGate.financeLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: run clarification QA before enabling. |
| FIN-005 | Stock-like table false positive | `주가처럼 변동표 만들어줘` | Table/document path; no finance lookup | Finance route execution | Release surface disabled until finance live QA passes | DISABLED | `ReleaseLiveProviderGate.financeLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: inspect false-positive execution log before enabling. |
| DART-001 | DART no key | `삼성전자 공시` without DART key | Direct personal DART key connection required | Cloudflare DART product path or fake empty success | Release surface disabled until DART BYOK live QA passes | DISABLED | `ReleaseLiveProviderGate.dartLiveQAPassed = false` | Reason: fail-closed release policy. Next: clear keychain and run no-key QA before enabling. |
| DART-002 | DART valid key | `삼성전자 공시` with valid DART key | Direct OpenDART result with official link | Cloudflare product route or fabricated disclosure | Release surface disabled until DART BYOK live QA passes | DISABLED | `ReleaseLiveProviderGate.dartLiveQAPassed = false` | Reason: fail-closed release policy. Next: run valid-key Direct BYOK QA before enabling. |
| DART-003 | DART stock code | `005930 공시` | Resolve to company/DART corp code or ask if ambiguous; official links | Treat stock code as DART corp code without validation | Release surface disabled until DART BYOK live QA passes | DISABLED | `ReleaseLiveProviderGate.dartLiveQAPassed = false` | Reason: fail-closed release policy. Next: run stock-code resolver QA before enabling. |
| DART-004 | Disclosure form false positive | `공시 양식처럼 써줘` | Document/form writing path, no DART lookup | DART lookup | Release surface disabled until DART BYOK live QA passes | DISABLED | `ReleaseLiveProviderGate.dartLiveQAPassed = false` | Reason: fail-closed release policy. Next: inspect false-positive execution log before enabling. |
| KMA-001 | Weather missing region | `날씨 알려줘` | Ask region; no Seoul default | Silent Seoul weather | Release surface disabled until KMA live QA passes | DISABLED | `ReleaseLiveProviderGate.kmaLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass missing-region QA before enabling. |
| KMA-002 | Gwangyang weather | `광양 날씨` | Weather for resolved Gwangyang grid, or honest provider error | Wrong region or fake success | Release surface disabled until KMA live QA passes | DISABLED | `ReleaseLiveProviderGate.kmaLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass Gwangyang weather live QA before enabling. |
| KMA-003 | Invalid credentials | KMA invalid_credentials state | Settings-needed failure | Success/empty weather | Release surface disabled until KMA live QA passes | DISABLED | `ReleaseLiveProviderGate.kmaLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: run bad-key failure QA before enabling. |
| NEWS-001 | Lithium news | `리튬 뉴스` | News search based on title/description and links | Article full-text summary claim | Release surface disabled until news live QA passes | DISABLED | `ReleaseLiveProviderGate.newsLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass news live QA before enabling. |
| NEWS-002 | News style false positive | `뉴스 기사처럼 써줘` | No news search; document writing path | News lookup | Release surface disabled until news live QA passes | DISABLED | `ReleaseLiveProviderGate.newsLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: inspect false-positive execution log before enabling. |
| NEWS-003 | Empty news request | `뉴스` or empty news query | Ask for keyword/topic; no economy default | Silent economy news | Release surface disabled until news live QA passes | DISABLED | `ReleaseLiveProviderGate.newsLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: run empty/ambiguous news request before enabling. |
| LAW-001 | Labor law annual leave | `근로기준법 연차 조문 찾아줘` | Law search result with official-source framing and not legal advice | Legal advice conclusion | Release surface disabled until law live QA passes | DISABLED | `ReleaseLiveProviderGate.lawLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: pass law live QA before enabling. |
| LAW-002 | Legal wording false positive | `법적으로 자연스럽게 써줘` | Rewrite/risk wording path; no law search | Law lookup | Release surface disabled until law live QA passes | DISABLED | `ReleaseLiveProviderGate.lawLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: inspect false-positive execution log before enabling. |
| LAW-003 | Empty law request | `법령 찾아줘` with no topic | Ask for law/topic; no Labor Standards Act default | Silent 근로기준법 default | Release surface disabled until law live QA passes | DISABLED | `ReleaseLiveProviderGate.lawLiveQAPassed = false`; `workerProductionHealthPassed = false` | Reason: fail-closed release policy. Next: run empty law request before enabling. |
| WORKER-001 | Worker source before deploy | Inspect Worker source/validators | Source validates; production response may still be previous deploy | Source validation presented as production deploy proof | Source-only checked by validator | PASS | `node --check workers/basic-lookup-api/worker.js`; release validator checks route manifest | Production deploy not asserted. |
| WORKER-002 | Worker production health | `python3 scripts/validate_worker_production_health.py` | health has `userRoutes` and `diagnosticRoutes`; `/dart/*` diagnostic only; expected version/build visible | DART in userRoutes, missing route split, stale version treated as deployed | Release public lookup surface disabled; live check currently fails because production `/health` returns `routes` but not `userRoutes`/`diagnosticRoutes` | DISABLED | `curl -fsS https://late-waterfall-c95c.urange.workers.dev/health` returned version `0.3.0`, build `public-lookup-0.3.0`, and `routes` only | Reason: fail-closed release policy. Next: redeploy repository Worker source, then run `python3 scripts/validate_worker_production_health.py` before enabling public lookup release gates. |

## Release Tag Rule

Release tag is forbidden until each live provider and Worker release-surface case is `PASS` or the relevant provider feature is explicitly `DISABLED` from release surface. This is enforced by `python3 scripts/validate_release_qa_evidence.py --release-strict`.
